
definitions:
  Bookmarks:
    type: "object"
    properties:
      id:
        type: string
        format: uuid
      userId:
        $ref: '#/definitions/User'
      articleId: 
        $ref: '#/definitions/Article'

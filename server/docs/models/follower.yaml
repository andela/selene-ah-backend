definitions:
  Follower:
    type: "object"
    properties:
      id:
        type: string
        format: uuid
      followerId:
        $ref: '#/definitions/User'
      userId: 
        $ref: '#/definitions/User'
